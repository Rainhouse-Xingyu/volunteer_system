# 系统接口文档

本文档列出了志愿服务系统的所有后端接口，按模块分类。

## 认证授权模块 (LoginController)

*   `POST /auth/login`: 用户登录
*   `POST /auth/register`: 用户注册
*   `POST /auth/logout`: 用户登出
*   `POST /auth/reset-password`: 重置密码
*   `GET /auth/info`: 获取当前登录用户信息
*   `POST /auth/password`: 修改密码

## 用户管理模块 (AdminController & UserUpdateController)

*   `GET /admin/users`: 获取用户列表
*   `PUT /admin/user/status`: 修改用户状态（启用/禁用）
*   `PUT /admin/activity/audit`: 审核活动 (AdminController, 待确认是否应在Activity模块)
*   `GET /admin/export/{activityId}`: 导出活动人员名单 (AdminController)
*   `GET /admin/audit/list`: 获取用户修改信息审核列表 (UserUpdateController)
*   `POST /admin/audit/{id}`: 处理用户修改信息审核 (UserUpdateController)

## 志愿者模块 (VolunteerController)

*   `GET /volunteer/me`: 获取志愿者个人信息
*   `POST /volunteer/update`: 更新志愿者信息（可能需要审核）
*   `GET /volunteer/stats`: 获取志愿者统计信息
*   `GET /volunteer/points/history`: 获取志愿者积分历史
*   `GET /volunteer/points/rank`: 获取志愿者积分排名
*   `GET /volunteer/my-registrations`: 获取我的报名记录
*   `POST /volunteer/checkin`: 志愿者签到 (也存在于 ActivityController)
*   `POST /volunteer/complete/{regId}`: 志愿者完成活动/确认完成
*   `GET /volunteer/activity-status/{activityId}`: 获取某活动的用户状态
*   `GET /volunteer/leaderboard`: 获取志愿者排行榜
*   `GET /volunteer/recommendation`: 获取活动推荐
*   `GET /volunteer/notifications`: 获取通知列表
*   `PUT /volunteer/notifications/{noticeId}/read`: 标记单条通知已读
*   `PUT /volunteer/notifications/read-all`: 标记所有通知已读

## 组织者模块 (OrganizerController)

*   `GET /organizer/me`: 获取组织者个人信息
*   `POST /organizer/update`: 更新组织者信息

## 活动模块 (ActivityController & RegistrationController)

*   `GET /activity/list`: 获取活动列表
*   `GET /activity/{id}`: 获取活动详情
*   `POST /activity/create`: 创建活动
*   `PUT /activity/update`: 修改活动
*   `DELETE /activity/{id}`: 删除活动
*   `GET /activity/my-list`: 获取我发布的活动列表（组织者）
*   `GET /activity/organizer/stats`: 获取组织者活动统计
*   `POST /activity/register/{activityId}`: 报名活动
*   `POST /activity/cancel/{activityId}`: 取消报名
*   `GET /activity/sign-token/{activityId}`: 获取签到码/Token
*   `POST /activity/checkin`: 签到 (也存在于 VolunteerController)
*   `GET /activity/registrations/{activityId}`: 获取活动报名人员列表 (等同于 `/registration/list/{activityId}`)
*   `PUT /activity/registration/audit`: 审核报名申请

*   `GET /registration/list/{activityId}`: 获取活动报名列表
*   `POST /registration/audit`: 审核报名
*   `POST /registration/checkin-code/{activityId}`: 生成签到码
*   `POST /registration/assess`: 评估/评价报名

## 新闻资讯模块 (NewsController)

*   `GET /news/list`: 获取新闻/动态列表
*   `GET /news/my-list`: 获取我发布的新闻列表
*   `GET /news/{id}` | `/news/detail/{id}`: 获取新闻详情
*   `POST /news/publish`: 发布新闻
*   `POST /news/delete/{id}`: 删除新闻

## 评价与评论模块 (CommentController & EvaluationController & CommentReportController)

*   `POST /comments/add`: 添加评论
*   `GET /comments/activity/{activityId}`: 获取活动评论列表
*   `GET /comments/news/{newsId}`: 获取新闻评论列表
*   `GET /comments/volunteer/my-comments`: 获取我的评论
*   `POST /comments/report`: 举报评论 (CommentReportController)
*   `GET /comments/reports`: 获取评论举报列表 (CommentReportController)
*   `PUT /comments/reports/{reportId}/process`: 处理评论举报 (CommentReportController)
*   `POST /evaluation/create`: 创建活动评价 (EvaluationController)

## 统计与分析模块 (StatisticsController & DataAnalysisController)

*   `GET /admin/statistics/overview`: 获取统计概览 (StatisticsController)
*   `GET /admin/statistics/charts`: 获取统计图表数据 (StatisticsController)
*   `GET /analysis/export/registrations/{activityId}`: 导出报名数据分析图表/数据 (DataAnalysisController)
*   `GET /analysis/chart/activity-status`: 获取活动状态图表数据 (DataAnalysisController)
*   `GET /analysis/chart/user-growth`: 获取用户增长图表数据 (DataAnalysisController)

## 违规管理模块 (ViolationController)

*   `POST /violation/report`: 举报违规
*   `GET /violation/list`: 获取违规列表
*   `PUT /violation/process`: 处理违规举报

## 通知模块 (NotificationController)

*   `POST /notifications/announce`: 发送系统通知/公告
*   `GET /notifications/my`: 获取我的通知
*   `POST /notifications/read/{id}`: 标记通知已读

## 文件上传模块 (FileUploadController)

*   `POST /file/upload`: 上传文件
